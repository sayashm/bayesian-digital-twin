# Thesis Completion Progress
**Last updated:** July 17, 2026 (**Day 11 complete**, continuing directly from Day 10 in the same session. Full 100-engine fleet comparison run: **one-shot decided as the thesis default** over periodic and pooled. Every remaining Day 10/11 writing item done: §3.4, §3.5 (NEW), §4.4-§4.7 (NEW: §4.7), §5.1-§5.3, and the Introduction (§1.3/§1.4) revised for the final results — all via the Day-8 checkpoint process. Two real gaps found and closed in-session rather than papered over: §3.4 (PMMH) was still a TODO stub despite Day-8's code/testing being done; and the full-fleet library comparison had never actually checked credible-interval coverage — checked directly, found it's still **0.000, unchanged by the point-accuracy fix** (`experiments/day10/day11_ci_coverage_finding.md`), which became the central finding shaping Chapter 5. Full end-to-end consistency pass done: braces/labels/refs/citations verified across every chapter, one real cross-table numeric inconsistency caught and explained (244.0 vs 244.3, RNG-seeding difference, footnoted in §4.7), and a full 5-chapter LaTeX syntax check run via an isolated wrapper (bypassing the two packages — `arev.sty`, `algorithm2e.sty` — this sandbox can't fetch without network access) confirmed **zero LaTeX errors**, all warnings expected (undefined refs/cites from a single-pass, no-bibtex test run). A local `latexmk` run against the real class file is still the standing outstanding item — same as prior sessions, cause now narrowed to exactly those two missing packages. **Cleanup needed on your machine:** `thesis/synctest.{aux,log,out,pdf}` — leftover from this verification, truncated to 0 bytes but not deletable from this sandbox (same FUSE-mount permission quirk documented for `.git/*.lock` files and `results.db-journal` on prior days — files this sandbox creates it sometimes cannot delete, even though it can overwrite/truncate them). See session log below for full detail.)
**Previous:** July 17, 2026 (Day 9 — closed. See session log below for full detail.)
**Process (current, Day 10 addition):** Claude now writes code directly (not just thesis prose, which changed Day 8) — Sajjad reviews/approves rather than writing-first. From today, implementation work also moves through Claude Code (the local CLI tool) for anything that needs to run on Sajjad's machine or is compute-heavy for the Cowork sandbox (e.g. the PMMH full-fleet run below) — Cowork (this session) hands off a self-contained prompt per step, Sajjad runs it locally and reports back so this file stays in sync. Cowork continues to handle planning, design decisions, thesis prose, and this progress file directly.
**Status legend:** ⬜ Not started | 🔄 In progress | ✅ Done | ❌ Blocked

---

## Admin & Milestones

| Item | Status | Target Date | Notes |
|------|--------|-------------|-------|
| Overleaf project created | ✅ | Day 1 | Live at Overleaf (Bayesian_DT_Thesis_Ayashm) |
| GitHub repo created | ✅ | Day 1 | https://github.com/sayashm/bayesian-digital-twin |
| SQLite schema designed | ✅ | Day 1 | 4 tables in database.py; schema verified |
| Supervisor report sent | ⬜ | Day 12 | 1–2 pages, one figure, one question |
| First draft to supervisor | ⬜ | July 15 | Required ≥1 month before defense |
| Poster pre-defense | ⬜ | TBD | Coordinate with Koen De Turck |
| Final submission (Plato) | ⬜ | ~Aug 10 | Required ≥2 weeks before defense |
| Public defense | ⬜ | Mid-Aug 2026 | |

---

## Code Modules (`particle_twin` package)

| Module | Status | Day | Notes |
|--------|--------|-----|-------|
| Package skeleton (`__init__.py`, folder structure) | ✅ | Day 1 | installed with pip install -e |
| `data/loader.py` — C-MAPSS reader (all 4 datasets) | ✅ | Day 1 | full implementation done |
| `data/database.py` — SQLite interface | ✅ | Day 1 (WAL mode added Day 7) | 4-table schema, verified. **Day 7:** `ResultsDB.__init__` now sets `PRAGMA journal_mode=WAL` on every connection — fixes a recurring stale-rollback-journal problem (`results.db-journal` left behind after any interrupted write, then blocking every subsequent open) hit repeatedly on this project, both from the Cowork sandbox mount and from DB Browser locally. |
| `features/health_index.py` — HealthIndexBuilder | ✅ | Day 2.5 | NEW. 3 methods: weighted, pca (sensor-only, fit/transform, usable train+test), industrial (RUL-based, train-only reference — cannot be used as a live observation) |
| `models/degradation_learner.py` — DegradationModelLearner | ✅ | Day 2.5 (bug fixed Day 3) | NEW. Fits mean dynamics on fleet-pooled (cycle, HI): linear / polynomial / exponential, selectable. Added `time=` param (pooled fit) and `predict(t)`; replaced hardcoded noise constant with explicit `sigma_v`. **Day 3 bug fix:** all three `dynamics_func` implementations drew process noise as a single shared scalar (`np.random.normal(0, sigma_v)`) applied identically to every particle, so resampled duplicate particles could never re-diversify. Fixed to `size=np.shape(health)` (independent noise per particle) after Sajjad noticed particle intervals/ESS were suspiciously static in `test_bootstrap.py`. |
| `models/measurement_learner.py` — MeasurementModelLearner | ✅ | Day 2.5 | NEW. Fits observation noise on residuals Y_t - E[X_t]: gaussian / empirical KDE / best-AIC-of-4 parametric family. |
| `models/state_space.py` — `DegradationModel` (v2) | ✅ | Day 2.5 | REWRITTEN. Orchestrates HI construction + degradation dynamics + measurement noise. Observation is now a scalar HI (`y = x + w`), not 21 raw sensors (`y = Cx + d + w`). API unchanged for the filter: `sample_initial`, `transition`, `log_likelihood`. See `experiments/test_state_space.py` for real-FD001 validation + model-comparison numbers. |
| `filters/bootstrap.py` — bootstrap particle filter | ✅ | Day 3 | Per-engine, not per-fleet. `BootstrapPF` class: init → predict → weight → resample (systematic, ESS-gated), all self-written by Sajjad line-by-line with review each step. Two real bugs caught and fixed during review: numpy `+=` aliasing (`self.history` entries all pointing at the same mutating array) and `logsumexp` misuse in `resample` (returned a scalar instead of the normalized weight array, collapsing every particle to one duplicated value each cycle). `normalize()` extracted as a shared helper used by both `ESS` and `resample`. |
| `analysis/rul.py` — RUL extraction from posterior | ✅ | Day 4 | Three functions, self-written by Sajjad step-by-step (same method as `bootstrap.py`) and reviewed line-by-line: `simulate_rul()` (vectorised Monte Carlo forward simulation — `alive`/`just_died` boolean masking, censored at `max_horizon`), `extract_rul()` (resamples the final particle cloud to an unweighted set before simulating, since the filter's own ESS-gated resample is not guaranteed to have fired on the exact last cycle; reports the exact pre-resample weighted `health_mean`), and `extract_rul_trajectory()` (loops `extract_rul()` over every observed cycle; dict fields match the `rul_estimates` DB schema exactly). Two real bugs caught during review: `h < threshold` instead of `<=` (would never fire, since `transition()` floors exactly at 0) and forward-simulating the still-weighted `h` instead of `h_resampled` in `extract_rul()`. Validated on real FD001 test-split engines (1/10/20/30) against `RUL_FD001.txt` — see Experiments below for the finding and `max_horizon` fix. |
| `analysis/metrics.py` — RMSE, ESS, MAPE, time | ✅ | Day 5 | Code works (final-cycle rmse/mape matching the `run_results` schema, full-trajectory rmse/mape, `ess_summary()`). Written by Claude unprompted (process miss — see Day 5 session log); Sajjad subsequently read the file independently, walked through the design piece by piece (pointwise errors → aggregators → ESS summary → ground-truth reconstruction → per-engine/fleet rollup) and confirmed understanding, including why final-cycle and trajectory error are both needed. PHM08 asymmetric score deliberately not implemented — not needed until Day 9. |
| `visualization/plots.py` — thesis-quality figures | ✅ | Day 5 | `plot_health_tracking()`, `plot_rul_distribution()`, `engine_summary_figure()`. Same process note as `metrics.py` — Claude-written, Sajjad reviewed and confirmed understanding independently afterward. |
| `inference/pmmh.py` — PMMH parameter estimation | ✅ | Day 8 | `log_prior_sigma_v` (half-normal, tau=0.3), `run_pf_for_sigma_v` (fresh `DegradationModel.fit()` + `BootstrapPF.run()` per candidate sigma_v), `pmmh_sample_sigma_v` (log-space RW-MH with explicit Jacobian term, since the prior is stated in sigma_v-space not u-space). `bootstrap.py`'s `run()` gained `self.log_likelihood_total`, accumulated via `logsumexp(after) - logsumexp(before)` bracketing the likelihood update each cycle (bracket must sit before the resample check, not after — resampling resets `log_w` to uniform, which would zero out that cycle's increment if measured post-resample). All three functions + the bootstrap.py addition written by Sajjad line-by-line, reviewed each step (see session log for the specific bugs caught). |
| Baseline model — linear/Kalman comparison | ✅ | Day 9 | NEW: `particle_twin/models/baseline.py` — `LinearRULBaseline` (ordinary least squares, pooled fleet fit on the same 10 sensors, point RUL prediction from the last observed cycle only). Written by Sajjad in 2 iterations after the math was explained (Claude reviewed, no bugs on the second attempt). `particle_twin/analysis/metrics.py` also gained `phm08_score`/`phm08_total_score` (PHM08 asymmetric competition score) and `ci_coverage` (credible-interval calibration check) — written by Sajjad step by step from a provided skeleton, 2 real bugs caught (missing `float()` wrap on a numpy scalar, breaking the file's own convention; and a genuine Python operator-precedence trap in the coverage mask, `lower <= true & true <= upper` silently NOT meaning what it looks like, since `&` binds tighter than `<=`). |
| `experiments/day10/similarity_library_prototype.py` — per-engine reference library + similarity matching | ✅ (prototype) | Day 10 | NEW. One exponential fit per training engine (reuses `DegradationModelLearner` unchanged, called per-engine instead of pooled — mean growth_rate=0.00373 vs. the pooled fit's 0.00276, confirming the dilution bias directly) + MMD-based similarity matching (RBF kernel, top-5 nearest averaged) + one-shot/periodic streaming variants, via a custom step-by-step filter loop that reuses `BootstrapPF`'s own normalize/ESS/resample methods untouched (no changes to `filters/bootstrap.py` or `models/state_space.py`). Validated on 5 held-out FD001 engines (1/10/20/30/77): mean final-RUL abs. error dropped from 233.8 (pooled baseline) to 139.6 (one-shot) / 144.0 (periodic) — every engine improved on every metric. Deliberately simplified for speed (regression-based rates not PF-PMMH, in-memory library not DB, top-5-averaged not top-1 match) — see the two rows below for what closes those gaps. Full writeup: `experiments/day10/day10_problem_solution.md`. |
| `particle_twin/inference/pmmh.py` — joint (growth_rate, sigma_v) extension | ⬜ | Day 10/11 | PLANNED, via Claude Code. Current `pmmh.py` only calibrates sigma_v (always against the pooled degradation fit, never touches growth_rate). Extension adds growth_rate as a second sampled dimension per engine, reusing the Day 10 prototype's model-swap mechanism instead of modifying `DegradationModel`/`DegradationModelLearner`. Needs: a prior for growth_rate (anchored near the library's observed mean 0.00373, std 0.00115), independent proposal step sizes per dimension, and a fallback rule (accept_rate < ~0.10 → use the regression-fit rate instead of an unconverged PMMH estimate — Day 8 found only 2/4 test chains mixed well). |
| `particle_twin/data/database.py` — schema addition for reference library | ⬜ | Day 10/11 | PLANNED, via Claude Code. None of the 4 existing tables (engines/experiments/run_results/rul_estimates) fit "per-engine model parameters" — needs a new table (engine_id, dataset, growth_rate, sigma_v, source [pmmh/regression-fallback], accept_rate, diagnostics). |

**New dependency:** `scikit-learn` (for PCA in `health_index.py`). Was already in `pyproject.toml`; added to `requirements.txt` too on Day 7 (versions pinned as a best guess — worth confirming against the actual `.venv` with `pip freeze | grep -iE "scikit|joblib|threadpool"`).

---

## Experiments

| Experiment | Status | Day | Notes |
|------------|--------|-----|-------|
| Single engine test (FD001, debug) | ✅ | Day 3 | `experiments/test_bootstrap.py` — runs 4 engines (1, 10, 20, 30), no crashes. **Final numbers (post degradation_learner.py noise fix):** filtered particle mean correlates 0.82–0.95 with observed HI (engine 1: 0.909, 10: 0.951, 20: 0.816, 30: 0.949); final unique-particle counts 300–500 out of 500 (healthy diversity); ESS shows a proper sawtooth pattern (climbs to N after each resample, decays to the τ·N=250 threshold, repeats) for the full trajectory on all 4 engines. Earlier same-day numbers (corr. 0.59–0.90, unique particles as low as 2–9) were from before the shared-noise bug fix and are superseded. Figures: `bootstrap_test.png` (particle cloud vs. observed HI), `bootstrap_ess.png` (ESS trace, resample threshold marked) — both regenerated post-fix. |
| RUL extraction on FD001 test engines (1/10/20/30) | ✅ | Day 4 | `experiments/test_rul.py` — fits model on train, runs `BootstrapPF` per test engine, calls `extract_rul_trajectory()`, stores every cycle to `results.db`, compares final-cycle RUL vs. `RUL_FD001.txt`. **Finding:** predicted RUL is systematically too high (e.g. engine 20: median 195 vs. true 16; all 4 engines off by 150–380 cycles) — traced (not assumed) to the §3.1 fleet-pooled-fit weakness by directly checking `degradation_learner.predict()`: from a healthy start (~93% health) the fitted curve alone needs ~960 cycles to reach failure, confirming the bias is inherited from the degradation model, not introduced by `rul.py`. Also found `max_horizon` itself was a confound: an initial 400 (and later a 200 test) was too small for the model's own shallow curve to ever finish from a healthy state, so predictions were hitting an artificial censoring ceiling rather than reflecting genuine model output; raised to 1500 to fix this. Figure `rul_test.png` (predicted median + 90% CI vs. implied ground-truth countdown, all 4 engines). Hit a sandbox-specific SQLite locking failure testing `results.db` storage from within the Cowork sandbox (stale `results.db-journal` + no fcntl support on the sandbox's mounted filesystem — reproduced even on a brand-new file, so not project-specific); resolved by running `test_rul.py` directly on Sajjad's machine, where it worked cleanly (`exp_id=4`, all 4 engines stored, 31/31 rows read back correctly for engine 1). |
| Full FD001 test set (all engines) | ✅ | Day 5 | `experiments/run_full_fd001.py` — all 100 test engines, resumable-chunk design (sandbox has no persistent background processes, so it processes N new engines per invocation and skips already-done ones on resume). **Fleet numbers (exp config: N=500, sigma_v=0.5, exponential/weighted/gaussian, same as Day 4):** fleet_rmse=244.0 cycles, fleet_mape=5.44 (544% — driven by engines with small true_rul, where even a moderate absolute error is a huge fraction), mean_trajectory_rmse=319.5, mean_ess_frac=0.785±0.02 (tight — filter health is consistently good across the whole fleet, not engine-dependent). Confirms the Day-4 4-engine finding holds at full-fleet scale, not a fluke of the 4 engines tested then. **Correction (verified 2026-07-10, re-checking before this update):** the script reported a `disk I/O error` on its DB-write step at the time, so it was assumed to have failed and `experiments/fd001_full_results.json` + `experiments/import_fd001_results.py` were built as a workaround. Re-checking `results.db` directly shows the sandbox write actually landed in full BEFORE the error (`exp_id=7`: 100/100 `run_results` rows, 13096/13096 `rul_estimates` rows — matches the JSON exactly). `import_fd001_results.py` has also since been run (`exp_id=23`, same row counts) — so `results.db` held this experiment **three times** (`exp_id` 7, 23, 24) as of Day 6. **Resolved Day 7:** duplicates `exp_id` 7 and 23 deleted, `exp_id=24` (100/100 engines, 13096/13096 rul_estimates rows) is now the single copy of this experiment. |
| PMMH parameter estimation (FD001) | ✅ | Day 8 | `experiments/test_pmmh.py`, engines 1/10/20/30. Diagnostic first: logZ noise at fixed sigma_v=0.3 measured directly (not assumed) across n_particles 200→800→3200→12800→25600 — std(logZ) fell from 22.1 to ~4, well short of the literature's std≈1 mixing target (would need an estimated 200k+ particles per MCMC step to reach it — impractical here); settled on n_particles=3200 as a documented computational-budget tradeoff, not a solved value. Final run: n_iterations=1000, rw_step=0.1. Mixing was engine-dependent: engines 10 and 30 reached healthy accept rates (0.336, 0.478) with engine 30 showing genuine bidirectional movement in its tail; engines 1 and 20 stayed mostly stuck (accept rates 0.051, 0.011) — their point posterior means are not trustworthy as converged estimates. Consistent finding across all 4 chains regardless of mixing quality: sigma_v posterior means (0.42–0.81) all sit above the assumed hardcoded default of 0.3, never below — a real, if imprecise, signal that the Day-2.5 default was too small. Engine-to-engine mixing variability (a single global rw_step isn't well-tuned for every engine's likelihood curvature) is an honest, reportable limitation for §3.4, not a bug. |
| Full FD001 systematic run, all metrics | ✅ | Day 9 | `experiments/day9/run_fd001_metrics.py` — reused Day 5's full 100-engine trajectories (`exp_id=24`, unchanged config) rather than re-running the filter, and added the two new metrics on top: **phm08_total_score=4.73e17, ci_coverage=0.000** (0/100 engines' 90% intervals contain the truth). Verified these aren't a metrics bug by hand-checking individual engines (e.g. engine 78: true\_rul=107, but even the interval's lower bound is 384.9 — the whole interval sits above the truth). Root cause is the same fleet-pooled over-prediction bias known since Day 4, now quantified precisely rather than just via RMSE. Diagnostic follow-up: reran FD001 with a **linear** degradation shape instead of exponential (`experiments/day9/run_fd001_linear.py`, `exp_id=27`, all 100 engines) to test whether curve shape drives the miscalibration — fleet\_rmse improved modestly (227.6 vs. 244.0) and phm08\_total dropped ~4 orders of magnitude (4.86e13), but **ci\_coverage stayed exactly 0.000**, unchanged. Conclusion: the calibration failure is not primarily about degradation-curve shape; it's most likely the fleet-pooling-by-absolute-cycle-number approach itself (§3.1's own caveat — different engines have different unknown initial wear, so pooling by raw cycle count can't distinguish a healthy engine at cycle 150 from a worn one at cycle 150, regardless of curve shape). |
| FD003 run (single condition, two fault modes) | ✅ | Day 9 | `experiments/day9/run_fd003.py`, `exp_id=26`, all 100 engines, identical config to `exp_id=24`. **Correction this session:** FD003 was mislabeled in `Thesis_claude.md`/`Thesis_timeline.md` as "multiple operating conditions" — `data/cmapss/readme.txt` confirms FD003 is actually ONE operating condition with TWO fault modes (HPC + Fan degradation); FD002/FD004 are the true multi-condition datasets (six conditions each), which this pipeline doesn't support (no operating-condition-aware normalisation) — both docs corrected, real multi-condition support noted as future work. Results: fleet\_rmse=227.6, fleet\_mape=6.67, mean\_ess\_frac=0.78, phm08\_total=4.74e13, ci\_coverage=0.000 — comparable in magnitude to FD001, so the second fault mode doesn't measurably worsen the filter beyond FD001's own baseline problems. |
| Baseline comparison | ✅ (compute) / ⬜ (write-up) | Day 9 / Day 10 | Baseline (`LinearRULBaseline`, same 10 sensors, OLS, pooled fleet fit) run on FD001: **fleet\_rmse=32.0, fleet\_mape=0.53, phm08\_total=16,121** — decisively better than the particle filter's 244.0 / 5.44 / 4.73e17 on every point-accuracy metric. This is a real, verified result (not a baseline bug), and complicates the planned §4.6 argument ("PF advantage is UQ, not just RMSE"): that argument needs the PF's own credible intervals to be trustworthy, and right now they aren't (0.000 coverage) — the PF has the *structural* capability for uncertainty quantification a point-estimate baseline categorically lacks, but the current pooled-fit configuration doesn't yet deliver a calibrated distribution in practice. Reframing this honestly, rather than overselling a "PF wins" narrative, is the plan for Day 10's §4.6 write-up. Actual numeric comparison table/write-up deferred to Day 10 as planned. |
| IDA/EDA → thesis artifacts | ✅ | Day 9 | NEW. Sajjad provided two existing analysis notebooks (`experiments/IDA/Improved_IDA.ipynb`, `Improved_EDA.ipynb`) documenting the original sensor-selection methodology (5-criterion composite ranking — Mann-Kendall significance fraction, monotonicity, trendability, mutual information, VIF — averaged across all 4 C-MAPSS sub-datasets, then VIF≤10 + pairwise-redundancy filtering on FD001) that had never been written down anywhere in the thesis or its docs. Built `experiments/IDA/export_thesis_artifacts.py` to re-derive just the needed pieces (not the full notebooks) and export thesis-ready outputs: `experiments/IDA/output/tables/dataset_dimensions.csv` (FD001 vs. FD003), `sensor_selection_fd001.csv` (full 14-sensor ranking), and two figures now in `thesis/Fig/` (`fig_sensor_composite_ranking.png`, `fig_selected_sensor_trajectories.png`). The script asserts its derived 10-sensor selection matches `SENSOR_COLS` exactly — caught one real discrepancy on the first run (threshold applied to FD001-only composite scores instead of the mean across all 4 datasets, which wrongly let `Nf` pass) before the numbers were used in the thesis text. FD002/FD004 composite scores are cached to disk (`experiments/IDA/output/tables/_cache/`) since computing them is too slow for one sandbox call. |
| Similarity-based library prototype, 5 engines | ✅ | Day 10 | `experiments/day10/similarity_library_prototype.py` — see Code Modules row above and `experiments/day10/day10_problem_solution.md` for the full table/writeup. Confirms the approach works; not yet at full-fleet scale or with PMMH-calibrated parameters. |
| Similarity-based library, full 100-engine fleet (regression-based) | ✅ | Day 11 | `experiments/day10/full_fleet_comparison.py`, all 100 FD001 test engines, chunked (4-5 engines/call) to fit the sandbox's 45s-per-call limit — regression-based library, same as the 5-engine prototype, NOT PMMH-calibrated yet. **Decision: one-shot is the thesis default** — wins on every fleet metric (mean abs.err 149.9 vs. periodic's 162.6 vs. pooled's 236.5; fleet RMSE 156.0 vs. 166.8 vs. 244.3; PHM08 total 9.19e12 vs. 9.25e12 vs. 6.90e17) and head-to-head on 63/100 engines vs. periodic's 28 (9 ties). Honest nuance kept, not hidden: periodic has lower error variance across the fleet (std 37.4 vs. one-shot's 43.1) — less accurate on average, slightly more consistent. Full writeup: `experiments/day10/day11_fullfleet_decision.md`. Two new figures: `thesis/Fig/fig_library_fullfleet_comparison.png`, `thesis/Fig/fig_engine78_library_before_after.png` (engine 78 before/after: pooled abs.err 410 → one-shot 188). |
| Similarity-based library, PMMH-calibrated (growth_rate, sigma_v jointly) | ⬜ | Day 11/12 | Still PLANNED, via Claude Code, run on Sajjad's machine (PMMH's MCMC cost doesn't fit the sandbox's 45s-per-call limit) — separate from the row above, which used regression-based rates only. Would refine the absolute numbers; unlikely to flip the one-shot-vs-periodic ranking since both variants share the same matching mechanism. |
| Final figures (thesis quality) | ⬜ | Day 11 | Moved from Day 10 — displaced by the new-way diagnosis/prototype work. |

---

## Thesis Chapters

### Chapter 1: Introduction
| Section | Status | Day | Notes |
|---------|--------|-----|-------|
| §1.1 Problem context (digital twins, predictive maintenance) | ✅ | Day 5 | Text in `chapt1.tex` (citation: `tuegel2011digitaltwin`). Written by Claude directly, unprompted — a process miss against `Thesis_claude.md` §4.1/§7 ("Sajjad writes the first attempt"). Flagged mid-Day-5; by Sajjad's explicit decision (2026-07-10, to stop losing time on it) it is being kept as-is rather than redrafted from scratch. Recorded here as an acknowledged exception, not the normal process — see session log and [[sajjad-collaboration-style]] memory. |
| §1.2 The limitation of classical approaches (point-estimate RUL) | ✅ | Day 5 | Same as §1.1 — citations `li2018cnnrul`, `zheng2017lstmrul` (verified). Kept as-is by the same explicit decision. |
| §1.3 Research objective | ✅ | Day 5, revised Day 11 | **Day 11 revision**, once final results were known (per the Day 11 timeline plan): softened "(ii) extracts a \emph{calibrated} RUL distribution" — that phrasing read as an achieved result rather than an open question, which turned out to matter once §4.7 found coverage still 0.000 — to "evaluate directly ... whether that distribution is calibrated." The "where it does not yet work" example was expanded from only the pooling-bias/RUL-bias finding to explicitly foreshadow the calibration-gap finding as well, since that turned out to be the more central, unresolved result. |
| §1.4 Thesis outline | ✅ | Day 5, revised Day 11 | `\label{ch:methodology}` added Day 5/7 so the cross-reference resolves. **Day 11 revision**: Chapter 3/4/5 descriptions updated to mention the similarity library (§3.5), the full-fleet library evaluation and calibration check (§4.7), and the point-accuracy-vs-calibration gap Chapter 5 now discusses. `\label{ch:intro}` added to `chapt1.tex` (was missing — needed for Chapter 5's back-reference). |

### Chapter 2: Background
| Section | Status | Day | Notes |
|---------|--------|-----|-------|
| §2.1 Bayesian inference (prior, likelihood, posterior) | ✅ | Day 6 | Prior/likelihood/posterior, Bayes' theorem, marginal likelihood as normalising constant, intractability of the integral, belief-updating vs. point-estimate argument (ties to RUL-as-distribution). Notation ($\theta$) flagged as generic placeholder, specialised later to $x_t$ (§3.1) and PMMH parameters (§3.4). |
| §2.2 Sequential Bayesian filtering & state space models | ✅ | Day 6 | Transition/observation equations, Markov property, why $x_t$ is "hidden" (never measured, only $y_t$), filtering problem $p(x_t\mid y_{1:t})$, recursive predict (Chapman–Kolmogorov)/update (Bayes) derivation, Kalman filter's 3 exactness conditions and why this thesis's model breaks them (nonlinear polynomial/exponential transitions; observation noise fit as gaussian/empirical/AIC-selected, not guaranteed Gaussian even for the linear case). |
| §2.3 Particle filters / SMC (SIR, bootstrap, resampling) | ✅ | Day 6 | Particle/weight representation, general importance-sampling weight ratio, SIR framework, bootstrap filter as the special case where proposal = transition prior (derived the cancellation explicitly), weight degeneracy, ESS formula with both extreme cases worked out (N vs. 1), adaptive resampling + systematic resampling, convergence (O(1/√N)) cited without proof. |
| §2.4 Digital twins in predictive maintenance | ✅ | Day 6 | Digital twin definition, physics-based/data-driven/hybrid taxonomy, this thesis classified as hybrid — corrected mid-session from an initial "physics-based dynamics" claim to the more precise grey-box/phenomenological framing (monotonic shape is physically motivated, curve parameters are data-fit). Particle filter framed as a principled *probabilistic* digital twin (posterior as internal representation) vs. deterministic twins. 2 new citations verified via web search: `rasheed2020digitaltwin` (taxonomy), `kapteyn2021pgm` (probabilistic digital twin framing). |
| §2.5 Related work — RUL estimation literature | ✅ | Day 8 | Written under the new Day-8 process (see §4.1 update in `Thesis_claude.md`) — Claude proposed content in plain language first, Sajjad approved, Claude wrote the `.tex` prose. 32 citations total in the section: 10 reused from earlier chapters (`chopin2020smc`, `kitagawa1996mcf`, `doccappe2005resampling`, `orchard2009particle`, `rasheed2020digitaltwin`, `kapteyn2021pgm`, `tuegel2011digitaltwin`, `li2018cnnrul`, `zheng2017lstmrul`, `gordon1993bootstrap`), 22 new to the bib today — `si2011rulreview` (found via direct web search, before Sajjad's uploads) plus 21 more (`arulampalam2002tutorial`, `fuller2020digitaltwin`, `li2017dbnwing`, `yu2020nonparametric`, `hosamo2023comfort`, `ademujimi2022dtbayes`, `dong2014socpf`, `wang2016sphericalpf`, `zhang2018exponentialpf`, `dong2018brownianpf`, `sbarufatti2017adaptivepf`, `eldalahmeh2023smoothpf`, `feng2022geardt`, `luo2020cncdt`, `xu2019transferdt`, `vandinter2022review`, `booyse2020deepdt`, `pittshephard1999apf`, `doucet2001smcpractice`, `soykan2025dapf`, `shi2026bearingpfdt`) sourced from two Sajjad-provided documents (`literature_map.md`/`corpus.csv`, an 784-work OpenAlex+arXiv corpus, and a synthesized `literature_review.md`), both produced by an external literature-search tool. Citations cross-checked against the corpus's own DOI records rather than taken from memory, except entries in `literature_review.md`, which Sajjad confirmed were already verified by that tool. One reference (`Jones et al. 2020`) mentioned in `literature_review.md` was NOT added — insufficient metadata (no DOI/journal given anywhere) to build a real bib entry. Verified: all 32 `\cite{}` keys used in `chapt2.tex` resolve in `Thesis_bib.bib`, braces balanced in both files. Could not run a full PDF compile in the Cowork sandbox (missing `arev.sty`, a font package unrelated to this content, no root access to install it) — **Sajjad still needs one local `latexmk` run to fully confirm the build**, same as the authoritative environment used on Day 7. |

### Chapter 3: Methodology
| Section | Status | Day | Notes |
|---------|--------|-----|-------|
| §3.1 State space model for turbofan degradation | ✅ | Day 7 (verified) | Tracker was stale — full prose (HI construction, degradation dynamics, observation model, initial distribution/parameter summary) is actually complete in chapt3.tex, fit-quality table included. Two placeholder citations still open: `\cite{[CITE: general SSM / prognostics reference]}` and `\cite{[CITE: wear-out / bathtub-curve reliability reference]}` — these will break the LaTeX build until real citations are added (afternoon reading-pass item). |
| §3.2 Bootstrap particle filter algorithm | ✅ | Day 3 | 4 subsections + intro, all drafted by Sajjad (Q&A-to-prose method for §3.2.3/§3.2.4) and reviewed line-by-line: SIR/bootstrap choice, filtering recursion (log-space, real -2000 log-likelihood number), ESS/adaptive resampling (real ESS-collapse number, systematic vs. multinomial corrected — Sajjad had conflated systematic with stratified/residual), algorithm summary + per-engine independence. New citations verified and added: gordon1993bootstrap, kitagawa1996mcf, doccappe2005resampling. Algorithm~1 (algorithm2e) documents `BootstrapPF.run()` exactly. |
| §3.3 RUL extraction from posterior | ✅ | Day 4 | 4 subsections + intro, drafted via the same Q&A-to-prose method as §3.2 and reviewed line-by-line: forward simulation as posterior prediction (two uncertainty sources — corrected Sajjad's initial framing, which referenced a "hidden fault" state left over from the Week-4 learning project's 3-component model; this thesis's SSM has no such variable), ensuring an unweighted particle set before simulating (identity-mapping argument for why resampling an already-uniform cloud is costless), Monte Carlo algorithm + censoring (Algorithm~2, `alg:rul`, mirrors Algorithm~1's style), posterior summary and decision relevance, and an honest limitations subsection connecting the Day-4 over-prediction finding back to §3.1's pooling caveat. New citation verified and added: `orchard2009particle` (Orchard & Vachtsevanos 2009 — standard particle-filter RUL prognosis reference). |
| §3.4 Parameter estimation via PMMH | ✅ | Day 11 | Gap closed same session it was found. Motivation (why a hand-set $\sigma_v$ isn't enough), pseudo-marginal MH theory (why substituting an unbiased likelihood estimate still targets the exact posterior — real risk of accidentally re-drawing $\widehat{Z}$ on rejection called out explicitly, since Sajjad had reasoned through exactly this point himself on Day 8), the log-likelihood estimator derived directly from `bootstrap.py`'s actual bracketing order (before the resample check, not after), prior/proposal (half-normal, log-space random walk, explicit Jacobian), the $N$-vs-$\mathrm{std}(\widehat{Z})$ diagnostic (22.1 at N=200 → ~4 at N=25,600, still short of the literature's std≈1 target; N=3200 as a documented trade-off), and the 4-engine results (accept rates 0.336/0.478 healthy, 0.051/0.011 stuck; all 4 posteriors 0.42-0.81, above the 0.3 default regardless of mixing quality). Two new citations added and verified via Crossref: `andrieu2010pmmh` (Andrieu, Doucet & Holenstein, 2010, JRSS-B — the PMMH method itself) and `andrieu2009pseudomarginal` (Andrieu & Roberts, 2009, Annals of Statistics — the pseudo-marginal validity result PMMH relies on). |
| §3.5 Similarity-based reference library (NEW) | ✅ | Day 11 | Per-engine library construction (§3.5.1), MMD similarity matching with RBF kernel (§3.5.2, Eq.~eq:mmd), one-shot/periodic streaming variants (§3.5.3), and the three deliberate simplifications vs. `cai2020similarity` (§3.5.4: scalar HI not full feature matrix; top-k-nearest not formal KTST; single growth-rate parameter not full RBPF state vector). New citation added and verified via Crossref + scite: `cai2020similarity` (Cai, Feng, Li, Hsu & Lee, 2020, *Applied Soft Computing* 94:106474 — the similarity-based PF paper this design mirrors). Which streaming variant to adopt as default is deferred to §4.7 by design (small-sample evidence wasn't decisive; full-fleet decision made there). |

### Chapter 4: Experiments & Results
| Section | Status | Day | Notes |
|---------|--------|-----|-------|
| §4.1 Dataset description (C-MAPSS) | ✅ | Day 9 | Written under the Day-8 process (Claude proposes plain-language content → Sajjad approves → Claude writes `.tex`). Covers FD001/FD003 (with the operating-condition/fault-mode correction above), train/test/RUL structure, and the full sensor-selection methodology sourced from Sajjad's IDA/EDA notebooks (Table~\ref{tab:dataset-dims}, Table~\ref{tab:sensor-selection}, 2 figures). New citation added and verified via web search: `mann1945nonparametric` (Mann, 1945, Econometrica — the Mann-Kendall trend test used in sensor selection). |
| §4.2 Evaluation metrics | ✅ | Day 9 | Same process. Final-cycle vs. trajectory error distinction, RMSE/MAPE, PHM08 asymmetric score (Eq.~eq:phm08), ESS (cross-referenced to Eq.~eq:ess in §3.2 rather than re-derived), and credible interval coverage (Eq.~eq:ci-coverage) — framed explicitly as the one metric a point-estimate baseline cannot be evaluated on at all, tying directly to \S4.6's argument. |
| §4.3 Experimental setup | ✅ | Day 9 | Same process. PF config (N=500, sigma\_v=0.5 hand-set, weighted HI, exponential primary + linear diagnostic), baseline config, reproducibility (seed, exp\_id 24/26/27), hardware/runtime. States plainly that PMMH was not integrated into any fleet-wide run this chapter and explains why (it only estimates sigma\_v, not the mean-dynamics shape parameters that actually drive the bias) — and that the planned N-sweep ({500,1000,2000}) was scoped out of this round by explicit decision. |
| §4.4 Results — health state estimation | ✅ | Day 11 | Filter tracking quality (correlation 0.82-0.95 on the 4 early diagnostic engines; Figure~fig:engine77-summary, reused from Day 5), fleet-wide ESS health (0.785±0.02, tight across all 100 engines — ruled out as the driver of per-engine difficulty), and the hardest-engine analysis (trajectory RMSE vs. lifespan, r=-0.68, p≈1e-14; shortest-lived quartile 367 vs. longest-lived quartile 245), tied explicitly back to §3.1's pooling caveat and forward to §4.7's fix. |
| §4.5 Results — RUL prediction | ✅ | Day 11 | Fleet RMSE=244.0/MAPE=5.44 under pooled dynamics, systematic over-prediction (Figure~fig:engine77-summary bottom panel), ci_coverage=0.000 with the engine-78 hand-check (interval lower bound >380 vs. true RUL 107), and the linear-vs-exponential diagnostic (exp_id 27) used to rule out degradation *shape* as the calibration driver. FD003 comparison included (comparable magnitude, second fault mode doesn't measurably worsen things). |
| §4.6 Results — comparison with baseline | ✅ | Day 11 | Table~tab:baseline-comparison (RMSE 32.0 vs. 244.0, MAPE 0.53 vs. 5.44, PHM08 16,121 vs. 4.73e17) reported plainly, then the Day-9-agreed UQ-capability-vs-current-calibration argument, framed honestly as "not yet realised" rather than a PF-wins narrative — explicitly hands off to §4.7 as the fix being tested. |
| §4.7 Results — similarity-based library vs. pooled baseline (NEW) | ✅ | Day 11 | Full 100-engine fleet table (Table~tab:library-fullfleet) and two figures (fig:engine78-library, fig:library-comparison). **One-shot adopted as thesis default** — wins every fleet metric, 63/100 engines head-to-head vs. periodic. Honest secondary finding kept in, not dropped: periodic has lower error variance (37.4 vs. one-shot's 43.1) despite the slightly worse mean. **Second gap found and closed same session:** the full-fleet comparison never checked `ci_coverage` for the library variants (only point-accuracy metrics) — computed it directly (`experiments/day10/library_ci_coverage.py`, new): **coverage is still 0.000 for one-shot, identical to pooled** (`experiments/day10/day11_ci_coverage_finding.md`). The library fix is a point-accuracy fix, not a calibration fix — §4.6's central "UQ capability not yet realised" argument still holds exactly as stated on Day 9, even after this fix. Added as its own paragraph in §4.7 (not buried or omitted) and carried into Chapter 5's framing. Uses regression-based library rates throughout (not yet PMMH-calibrated — flagged as future work, §5.3). |

### Chapter 5: Discussion & Conclusions
| Section | Status | Day | Notes |
|---------|--------|-----|-------|
| §5.1 Discussion (interpretation, limitations) | ✅ | Day 11 | 5 subsections: what the PF delivers (health tracking, library fix confirms the pooling diagnosis); the central limitation stated head-on (point accuracy fixed, coverage still 0.000 — two separate problems, not two symptoms of one); likely sources (hand-set $\sigma_v$, PMMH never carried past 4 engines to fleet scale); other limitations (matching-method simplifications vs. `cai2020similarity`, engine-dependent PMMH mixing, no FD002/FD004); connection back to the digital-twin framing (architecture confirmed, calibration not yet). |
| §5.2 Conclusions | ✅ | Day 11 | Answers the research objective directly: yes on point-accuracy/architecture, no (not yet) on calibration — coverage 0.000 throughout. Practical implication for maintenance engineers stated in plain terms: trust the corrected point prediction more, don't yet trust the stated interval bounds. |
| §5.3 Future work | ✅ | Day 11 | 6 items, ordered by how directly each targets the calibration gap (not by ease): fleet-wide PMMH calibration of $\sigma_v$; joint PMMH extension (growth_rate + $\sigma_v$, handoff prompt already exists); life-fraction-normalised pooling (the other untried fix for the original bias); closing the two `cai2020similarity` simplifications (formal KTST, full feature matrix); FD002/FD004 support; the N-particle sweep scoped out on Day 9. |

### Front/Back Matter
| Item | Status | Day | Notes |
|------|--------|-----|-------|
| Abstract (English) | ⬜ | After full draft | Written last |
| Acknowledgements | ⬜ | Final week | |
| References / Bibliography | 🔄 Ongoing | — | Add citations as you write |
| Appendix — code architecture | ⬜ | Final week | Optional |

---

## Writing Quality Checklist (per section before marking ✅)

- [ ] Mathematical notation is precise and consistent throughout
- [ ] Every claim from a source has a citation (APA style)
- [ ] Uncertainty is quantified — no results stated as bare point estimates
- [ ] Figures are labeled (caption, axes, units)
- [ ] Section connects to thesis argument (how does this support "PF is a better digital twin"?)
- [ ] Plagiarism check: all paraphrased ideas cited (Ephorus will scan the submission)

---

## Session Log
*(Update at end of every working session)*

| Date | What was done | Next session starts with |
|------|--------------|--------------------------|
| 2026-06-28 | Planning session — all 4 guide files created | Day 1: infrastructure setup |
| 2026-06-29 | Day 1 complete: particle_twin package (loader.py, database.py, all stubs); SQLite schema verified; thesis_outline.md; thesis/ LaTeX on Overleaf; GitHub repo live at sayashm/bayesian-digital-twin; .venv created | Day 2: design degradation state space model on paper first, then implement state_space.py |
| 2026-07-01 | Day 2 complete: state space math derived; DegradationModel implemented (fit, sample_initial, transition, log_likelihood); loader.py updated to physical sensor names + unit_id; synthetic test passed (experiments/test_state_space.py); §3.1 all 5 paragraphs written and in chapt3.tex | Day 3: implement bootstrap.py particle filter; run on one FD001 engine |
| 2026-07-06 | Day 2.5 (architecture revision, before starting Day 3): replaced the single linear/21-sensor model with a 3-model pipeline built from Sajjad's own prototype classes (HealthIndexBuilder, DegradationModelLearner, MeasurementModelLearner). New `features/health_index.py` (weighted/pca/industrial HI), `models/degradation_learner.py` (linear/polynomial/exponential dynamics, now fittable on pooled fleet data via `time=`), `models/measurement_learner.py` (gaussian/empirical/custom noise). `models/state_space.py` rewritten: observation is now `y=x+w` (scalar HI) instead of `y=Cx+d+w` (21 sensors). Validated end-to-end on real FD001 data (`experiments/test_state_space.py`) — R² comparison across 2 HI methods × 3 degradation shapes, AIC comparison for measurement noise; `state_space_test.png` regenerated. chapt3.tex §3.1 restructured with new equations + honest fit-quality discussion (pooling-by-absolute-cycle caveat); original Day-2 prose kept as a comment. **Still needs:** Sajjad to draft the prose for the new §3.1 structure; pick final default (hi_method, degradation_model, measurement_method) combination for the results chapter, informed by the comparison table already in chapt3.tex. | Day 3 (as planned): implement bootstrap.py — no changes needed to its intended interface, it consumes `DegradationModel.sample_initial/transition/log_likelihood` exactly as before |
| 2026-07-08 | **Day 3 complete.** `filters/bootstrap.py` (`BootstrapPF`) implemented by Sajjad step-by-step (init → predict → weight → resample), reviewed at each step: caught and fixed a numpy `+=` aliasing bug (all `self.history` weight entries pointing at the same mutating array) and a `logsumexp` misuse in `resample` (scalar instead of normalized array, collapsing every particle to one duplicated value per cycle). Ran on FD001 engines 1/10/20/30 (`experiments/test_bootstrap.py`); found and fixed a real bug in Day-2.5's `degradation_learner.py` (process noise was a single shared scalar applied to every particle, not independent per particle) — after the fix, filtered-vs-observed correlation improved from 0.59–0.90 to 0.82–0.95 and particle diversity from single digits to 300–500/500. Figures `bootstrap_test.png` / `bootstrap_ess.png` regenerated. Wrote thesis §3.2 (all 4 subsections + intro) via a Q&A-to-prose method for the last two subsections (Sajjad answered plain-language questions, Claude corrected/formalized); caught a real conceptual error (Sajjad had conflated systematic resampling with stratified/residual — they're distinct schemes). Added and verified 3 new citations: `gordon1993bootstrap`, `kitagawa1996mcf`, `doccappe2005resampling`. | Day 4: implement `analysis/rul.py` — propagate the final particle cloud (cycle T) forward until a failure threshold, extract RUL as a distribution (median + 90% CI), not a point estimate; then write thesis §3.3 |
| 2026-07-10 | **Day 5 complete.** `particle_twin/analysis/metrics.py` implemented (final-cycle RMSE/MAPE matching the `run_results` schema + literature convention, full-trajectory RMSE/MAPE as a richer diagnostic, `ess_summary()`). Ran `experiments/run_full_fd001.py` on all 100 FD001 test engines (not just 4) — resumable-chunk design to work around the sandbox's inability to keep a background process alive between tool calls. **Fleet result:** fleet_rmse=244.0, fleet_mape=5.44, mean_ess_frac=0.785±0.02 — confirms the Day-4 4-engine over-prediction finding holds at full scale. **Hardest-engine analysis** (Pearson correlations across all 100 engines): trajectory_rmse correlates strongly negatively with n_cycles observed (r=-0.68, p=1e-14) and with approximate total lifespan (r=-0.68, p=1e-14) — short-lived (fast-failing) engines are the hardest, exactly as the §3.1 pooling-by-absolute-cycle caveat predicts, now measured directly rather than eyeballed from 4 engines. Quartile check: shortest-lived 25% of engines average trajectory_rmse=367 vs. 245 for the longest-lived 25%. Ruled out particle degeneracy as an alternative explanation: mean_ess_frac is tight across the whole fleet (0.74-0.84), so the weak positive correlation between ESS and error (r=+0.32) is a confound of both correlating with n_cycles, not an independent driver — the problem is the degradation model, not the filter. Built the first thesis-quality figure (`particle_twin/visualization/plots.py` — new reusable module, `experiments/make_figure_engine77.py` uses it) for engine 77, chosen because its trajectory_rmse (314.9) sits almost exactly at the fleet median (316.4) — deliberately not a cherry-picked best case. Figure saved to `thesis/Fig/fig_engine77_health_rul.png`. Hit the same sandbox SQLite `disk I/O error` as Day 4 (confirmed it's not fixed by `journal_mode=MEMORY` either); worked around it this time with `experiments/fd001_full_results.json` (always written) + `experiments/import_fd001_results.py` (fast local DB import, no recompute) so results.db doesn't block on being run locally — **Sajjad still needs to run `import_fd001_results.py` once locally to get these 100 engines into `results.db`.** Wrote thesis Introduction §1 (all 4 subsections) directly rather than via Q&A-to-prose (motivate/gap/objective writing, not a technical derivation to walk through). 3 new citations added and verified via web search (author/venue/year/pages checked, not taken from memory): `tuegel2011digitaltwin` (digital twin concept), `li2018cnnrul` (CNN RUL baseline), `zheng2017lstmrul` (LSTM RUL baseline). Added the missing `\label{ch:methodology}` to chapt3.tex so chapt1.tex's forward-reference resolves. | Day 6: Background chapter (§2.1-§2.5) — Bayesian inference, state space models/filtering, particle filters/SMC, digital twins in predictive maintenance, related work. Also: run `experiments/import_fd001_results.py` locally to populate `results.db` with the full 100-engine run. |
| 2026-07-10 (same day, follow-up) | **Day 5 process correction.** The Day-5 entry above was produced by Claude end-to-end without checkpoints — `Thesis_claude.md` (the actual working-agreement file) was never read at session start, only `Thesis_completing_Progress.md`/`Thesis_timeline.md` were. This directly violated §4.1/§4.2/§7 ("Sajjad writes the first attempt", "never write a full section unprompted"). Sajjad caught it ("I don't know what did you do"). Recovery: (1) re-verified every Day-5 claim against the actual files/DB rather than trusting the original notes — found and corrected one real error (the sandbox `results.db` write was reported as failed but had actually landed in full; `results.db` now holds the Day-5 experiment twice, `exp_id=7` and `exp_id=23`, harmless duplicate, not cleaned up); (2) walked `metrics.py` through piece by piece with Sajjad (pointwise errors → aggregators → ESS summary → ground-truth reconstruction → per-engine/fleet rollup) until he confirmed understanding, then he independently read `metrics.py` and `plots.py` in full himself; (3) by Sajjad's explicit decision, `chapt1.tex` (§1.1-1.4) is being kept as Claude-authored rather than redrafted from scratch, to stop losing time — recorded here as a deliberate, acknowledged exception, not the normal process. **Standing rule going forward:** read `Thesis_claude.md` at the start of every session; do not produce a finished deliverable (code or prose) without a checkpoint first. See [[sajjad-collaboration-style]] memory for the full incident. |
| 2026-07-10 | **Day 4 complete.** `particle_twin/analysis/rul.py` implemented by Sajjad step-by-step, same method as Day 3 (algorithm explained conceptually first, each function written and reviewed line-by-line before moving on): `simulate_rul()` (vectorised forward Monte Carlo, `alive`/`just_died` masking, worked through with a hand-traced 3-particle numeric example before Sajjad wrote the real version) → `extract_rul()` (weighted `health_mean` + resample-before-simulating) → `extract_rul_trajectory()`. Two real bugs caught during review: a `<` vs `<=` threshold comparison that would never fire (`transition()` floors exactly at 0, so `h < 0` is never true) and forward-simulating the still-weighted particle array instead of the resampled one. Tested on real FD001 test-split engines (1/10/20/30) against `RUL_FD001.txt`: predicted RUL is systematically ~150–380 cycles too high across all four engines, traced directly to the §3.1 fleet-pooled-fit weakness (confirmed via `degradation_learner.predict()`, not assumed) rather than a `rul.py` bug. Also found and fixed a related `max_horizon` confound: 400 (and later a user-set 200) was too small for the model's own already-too-shallow fitted curve to ever finish from a healthy starting state (~960 cycles needed) — raised to 1500 so predictions reflect genuine model output rather than an artificial censoring ceiling. Hit a sandbox-specific SQLite locking failure testing `results.db` storage from within the Cowork sandbox (stale journal + no fcntl support on the mount, reproduced even on a brand-new file so confirmed environment-specific, not project-specific); resolved by running `experiments/test_rul.py` directly on Sajjad's machine, where it worked cleanly (`exp_id=4`, all 4 engines stored, 31/31 rows read back correctly). Wrote thesis §3.3 (4 subsections + intro) via Q&A-to-prose; caught a real conceptual error (Sajjad's initial uncertainty-sources answer referenced a "hidden fault" state that doesn't exist in this thesis's SSM — leftover from the Week-4 learning project's 3-component model, not this project's scalar Health Index model). Added and verified 1 new citation: `orchard2009particle`. | Day 5: run PF on the full FD001 test set (not just 4 engines), compute fleet RMSE, build the first thesis-quality figure (health tracking + RUL distribution), tune noise parameters, write Introduction §1 draft |
| 2026-07-13 | **Day 6 complete.** Background chapter §2.1–§2.4 all written in `chapt2.tex`, replacing the TODO scaffolding, via Q&A-to-prose at Sajjad's explicit request ("can we do it like Q&A that we did before?") for the whole day, not just the harder subsections. §2.1 (Bayesian inference): prior/likelihood/posterior, Bayes' theorem, $P(y)$ corrected from "comes from the sensors" to the marginal-likelihood/normalising-constant definition, intractability, and the point-estimate-vs-distribution argument pushed to a concrete engineer decision (act on the lower credible bound, not the median). §2.2 (state space/filtering): transition+observation equations, Markov property (Sajjad raised this himself, unprompted), corrected "hidden" from a vague "unknown" to the precise never-directly-measured definition, filtering problem $p(x_t\mid y_{1:t})$ corrected from an initial "compute the likelihood" answer, Chapman–Kolmogorov predict step built by analogy after Sajjad asked for help, update step reached correctly via the weights/likelihood hint, Kalman-filter exactness conditions and why this thesis's model breaks them (Sajjad correctly identified that even the *linear* transition shape doesn't guarantee exactness, since the measurement noise model can be non-Gaussian regardless of shape). §2.3 (particle filters/SMC): particle representation, general importance-weight ratio, bootstrap filter's proposal-cancellation derived correctly and precisely by Sajjad unassisted, ESS formula with both extremes computed explicitly (N and 1), degeneracy, adaptive/systematic resampling, convergence cited without proof. §2.4 (digital twins): digital twin definition + physics-based/data-driven/hybrid taxonomy; Sajjad initially misclassified the degradation dynamics as "physics-based" — corrected via a targeted question pointing back at `degradation_learner.py` (OLS/quadratic/log-linear regression, no thermodynamic equations) to the more precise grey-box/phenomenological framing (physically-motivated monotonic *shape*, data-fit *curve*); Bayesian filter framed as a principled *probabilistic* digital twin via the posterior distribution. 2 new citations added and verified via web search (not from memory): `rasheed2020digitaltwin` (IEEE Access, 2020 — physics-based/data-driven/hybrid taxonomy), `kapteyn2021pgm` (Nature Computational Science, 2021 — probabilistic graphical model framing of digital twins). Verified after writing: every `\cite{}` key in `chapt2.tex` resolves in `Thesis_bib.bib`, and both files have balanced braces. **Not yet done:** Sajjad has not yet re-read/edited §2.1–§2.4 in his own words (the sections above are Claude-formalized prose from his Q&A answers, same status as §3.2.3/§3.2.4/§3.3 were before his independent read-through) — that pass should happen before checking the writing-quality checklist boxes. §2.5 (Related Work) is Day 8, not today, per the existing chapt2.tex TODO split. **Still outstanding from Day 5:** Sajjad still needs to run `experiments/import_fd001_results.py` locally to get the full 100-engine FD001 run into `results.db`. | Day 7: Review & Consolidate — morning: review all Week-1 code for outstanding bugs; afternoon: read all written sections (chapt1.tex, chapt2.tex §2.1–2.4, chapt3.tex §3.1–3.3) end to end and mark gaps, weak arguments, missing citations, starting with Sajjad's own read-through of today's §2.1–2.4. |
| 2026-07-13 (same day) | **Day 7 complete.** Morning (code review): read through every `particle_twin` module (`loader.py`, `database.py`, `health_index.py`, `degradation_learner.py`, `measurement_learner.py`, `state_space.py`, `bootstrap.py`, `rul.py`, `metrics.py`, `plots.py`) — no bugs found; stress-tested `bootstrap.py`'s `resample()` for a theoretical out-of-bounds `searchsorted` edge case (300k trials, never occurred, no fix needed). Cleaned up two Day-5/6 loose ends: added scikit-learn/joblib/threadpoolctl to `requirements.txt` (already in `pyproject.toml`); deleted the duplicate Day-5 FD001 experiment rows from `results.db` (`exp_id` 7 and 23, keeping 24) — this took much longer than expected because of a genuine recurring problem, not a one-off: my first attempt (from the Cowork sandbox) hit the same `disk I/O error` documented on Days 4/5 and left a stale `results.db-journal` neither the sandbox nor, initially, Sajjad's own DB Browser could clear (DB Browser showed a false "SQLCipher encryption" password prompt, actually just a symptom of the stuck journal). Resolved by having Sajjad delete the journal and run the DELETE via the plain `sqlite3` CLI locally instead of DB Browser's GUI (whose "Write Changes" step wasn't reliably committing). **Permanent fix applied:** `database.py`'s `ResultsDB.__init__` now sets `PRAGMA journal_mode=WAL` on every connection, which avoids the rollback-journal mechanism causing this in the first place. Afternoon (reading pass): read chapt1.tex, chapt2.tex §2.1–2.4, chapt3.tex §3.1–3.3 end to end — content and citations are solid (every real `\cite{}` key resolves), only gap is the known §2.5/§3.4 TODOs (Day 8) and two placeholder citations in chapt3.tex §3.1. Also discovered the tracker's §3.1 status was stale (marked in-progress; prose was actually already complete) — corrected. Bigger finding: **the thesis did not compile at all** — `main.pdf` had never been produced. Root-caused and fixed a chain of local TinyTeX gaps over several rounds with Sajjad (missing `ifoddpage.sty`, `relsize.sty`, font/graphics collections, a stale-latexmk-cache issue requiring manual cache clears between fixes) and one real bug in `Thesis_bib.bib` (a template comment containing a literal `@article{...}` shape was being parsed by BibTeX as a real malformed entry, since `.bib` has no true comment syntax — my own first fix attempt repeated the same mistake before being caught). The two placeholder citations were commented out with `TODO-CITE` markers (Sajjad's choice, over finding real sources immediately) so the build isn't blocked on them. **End state, verified:** `main.pdf` compiles cleanly, 41 pages, all 12 bibliography entries render as proper author-year citations, zero undefined citations or references anywhere in the log. | Day 8: PMMH teaching session + `inference/pmmh.py` implementation, then Related Work §2.5. Also: find real citations for the two `TODO-CITE` markers in chapt3.tex §3.1 before the next full compile check, and confirm the `requirements.txt` scikit-learn/joblib/threadpoolctl version pins actually match the local `.venv`. |
| 2026-07-17 | **Day 8 complete, compressed process.** Sajjad flagged limited remaining time and explicitly changed the standing writing-coaching rule in `Thesis_claude.md` §4.1: superseded the "Sajjad writes first" process; new process is Claude proposes section content in plain language → Sajjad approves/corrects the content → Claude writes the `.tex` academic prose directly → Sajjad reviews section-by-section. Recorded as a feedback memory ([[sajjad-collaboration-style]]) since this is a deliberate, explicit, time-driven exception, not abandonment of his general preference to write things himself. **PMMH:** taught via 5 segments (definition/why point-estimate params aren't enough → MH acceptance ratio + pseudo-marginal validity → deriving the log-likelihood estimator from `bootstrap.py`'s actual accumulation style, not the textbook every-step-reset version → worked numeric example → full MH loop incl. log-space positivity fix) — Sajjad answered every comprehension check correctly, including reasoning through the pseudo-marginal "must reuse old logZ on rejection" rule himself. `bootstrap.py` gained `log_likelihood_total` (Sajjad wrote it himself in 3 iterations, 2 real bugs caught: folding the scalar increment into the per-particle `log_w` array instead of a separate scalar, and measuring the "after" `logsumexp` post-resample instead of pre-resample). `inference/pmmh.py` (`log_prior_sigma_v`, `run_pf_for_sigma_v`, `pmmh_sample_sigma_v`) was largely Claude-written after Sajjad's own attempts at each function (explicit requests, given time pressure), but Sajjad drove the design questions himself (e.g. correctly identified that a hyperparameter of the *prior* on sigma_v shouldn't itself be PMMH-estimated, since it doesn't touch the particle filter's likelihood at all). **PMMH testing:** diagnosed a "sticky chain" pathology (falling accept rate as rw_step *shrank*, the opposite of plain-MH intuition) as pseudo-marginal-specific likelihood-estimator noise, not a proposal-tuning problem — measured std(logZ) directly across n_particles 200→25600 rather than guessing, found the literature's std≈1 mixing target would need an impractically large N, and made an explicit, documented computational-budget tradeoff (n_particles=3200) rather than chasing the ideal. Final result: mixing was engine-dependent (2/4 engines healthy, 2/4 stuck), but all 4 engines' sigma_v posteriors sit above the assumed 0.3 default, a real if imprecise signal. **§2.5 Related Work:** Sajjad uploaded three literature-search-tool outputs (`literature_map.md`, `corpus.csv` — a 784-work OpenAlex+arXiv corpus — and `literature_review.md`); asked for "comprehensive" coverage (~18-20 new citations) when offered a scope choice. 21 new citations added and cross-checked against the corpus's own DOI records (one exception: `literature_review.md`'s citations taken as pre-verified per Sajjad's explicit instruction, since that file was itself produced by a citation-checking tool). One mentioned reference (Jones et al. 2020) was deliberately NOT added — no DOI/journal given anywhere, insufficient to build a real entry. Verified all 32 `\cite{}` keys in `chapt2.tex` resolve and braces balance in both files; could not do a full PDF compile in this sandbox (missing `arev.sty`, no root access) — a local `latexmk` run is still needed to fully confirm the build. | Day 9 (per timeline): full FD001 systematic run with all metrics logged, FD003 run (multiple operating conditions), implement a simple baseline (e.g. linear regression) for comparison, and start Experiments chapter §4.1-4.3 (dataset, evaluation metrics, experimental setup). Given today's compressed process is likely to continue, confirm with Sajjad at Day 9's start whether the plain-language-propose-then-write process should carry forward, and whether the two PMMH-mixing limitations (engine-dependent accept rates, std(logZ) not reaching the literature target) should be written up in §3.4 before or after Day 9's experiments. |
| 2026-07-17 (Day 9) | **Day 9 complete.** Confirmed at session start: Day-8 writing process continues; §3.4's PMMH limitations write-up deferred until after today's experiments (done). **Metrics:** `phm08_score`/`phm08_total_score`/`ci_coverage` added to `metrics.py`, written by Sajjad from a provided skeleton, reviewed step by step (2 real bugs: missing `float()` wrap on a numpy scalar; a genuine `&`/`<=` operator-precedence trap in the coverage mask). **FD001 (`exp_id=24`, reused, all metrics added):** phm08\_total=4.73e17, ci\_coverage=0.000 — verified by hand, not a bug; e.g. engine 78's entire 90% interval (384.9–715.2) sits above its true RUL of 107. **FD003 (`exp_id=26`, new, 100 engines):** comparable magnitude to FD001 (rmse=227.6, ci\_coverage=0.000) — the second fault mode doesn't measurably worsen the filter. **Correction:** FD003 was mislabeled in `Thesis_claude.md`/`Thesis_timeline.md` as "multiple operating conditions" (it's actually one condition, two fault modes; FD002/FD004 are the real multi-condition sets) — both docs fixed. **Diagnostic (`exp_id=27`):** reran FD001 with a linear instead of exponential degradation shape to test whether curve shape drives the miscalibration — RMSE improved modestly (227.6 vs. 244.0), phm08\_total dropped ~4 orders of magnitude, but ci\_coverage stayed exactly 0.000 — ruling out curve shape as the calibration driver; the fleet-pooling-by-absolute-cycle approach itself (§3.1's own caveat) is the more likely cause. **Baseline:** `particle_twin/models/baseline.py`'s `LinearRULBaseline` (OLS, same 10 sensors, pooled fleet fit), written by Sajjad in 2 iterations, no bugs on the second attempt. Result was a real, verified surprise: fleet\_rmse=32.0 vs. the PF's 244.0 — the baseline wins decisively on every point-accuracy metric. This prompted a direct discussion with Sajjad about what this means for the thesis's core argument; agreed framing: the PF's advantage has to rest on its *structural* capability for calibrated uncertainty quantification (which a point baseline categorically lacks), not on point accuracy, and the current 0.000 coverage means that capability isn't yet realised in practice — an honest limitation for §4.6/Discussion, not a result to paper over. **IDA/EDA:** Sajjad surfaced two existing notebooks (`Improved_IDA.ipynb`, `Improved_EDA.ipynb`) documenting the original sensor-selection methodology, never previously written into the thesis or docs. Built `experiments/IDA/export_thesis_artifacts.py` to re-derive and export the needed tables/figures; caught and fixed one real discrepancy (composite-score threshold must be averaged across all 4 C-MAPSS sub-datasets, not FD001 alone) via a self-check assertion before the numbers reached the thesis text. **Writing (§4.1–4.3):** all three written into `chapt4.tex` under the Day-8 process, using the real numbers/tables/figures from today's experiments and the IDA/EDA notebooks; 1 new citation added and verified via web search (`mann1945nonparametric`). Braces balanced and all citations/cross-references verified to resolve in every file touched today; could not run a full local `latexmk` compile this session (sandbox environment issue) — **a local compile check is still needed**, same standing item as prior sessions. | Day 10 (per timeline): create all thesis-quality figures (health tracking, RUL distributions, RMSE/PHM08 comparison, ESS plots); statistical analysis of RMSE distribution across engines (hard vs. easy engines, as in the Day-5 hardest-engine analysis); write Results §4.4 (health state estimation) and §4.5 (RUL prediction) with every figure getting a paragraph of interpretation; write §4.6 (baseline comparison) using today's numbers, with the reframed UQ-capability-vs-current-calibration argument agreed today rather than a simple "PF wins" narrative. Also: run a local `latexmk` compile to confirm chapt4.tex's new tables/figures render correctly. |
| 2026-07-17 (Day 10) | **Day 10, in progress — new-way design agreed, original writing tasks moved to Day 11.** Diagnosed and quantified the Day-9 PHM08 blowup: pooled growth_rate (0.00276/cycle) sits ~26% below the fleet's own per-engine average (0.00373, std 0.00115, from individually fitting all 100 training engines) — pooling by absolute cycle number across engines with very different lifespans dilutes the fitted decay rate, so every particle takes too long to reach failure, and PHM08's asymmetric scoring (over-prediction punished harder than under-prediction) turns that bias into the 4.7e17 fleet score. Sajjad independently proposed the fix — a per-engine reference library + similarity matching, the same core idea as this thesis's main comparison paper (Cai et al. 2020) — and separately raised the real-time/streaming question (test data arrives cycle-by-cycle for an actual digital twin, not as a pre-collected batch) unprompted. Built and validated a small prototype (`experiments/day10/similarity_library_prototype.py`, 5 held-out engines: 1/10/20/30/77): mean final-RUL abs. error dropped from 233.8 (pooled baseline) to 139.6 (one-shot match) / 144.0 (periodic re-match), PHM08 scores fell 3-11 orders of magnitude, every engine improved on every metric — confirms the approach works, at small scale, with several deliberate simplifications. Sajjad then shared two whiteboard photos (OLD WAY / NEW WAY diagrams) of his own design; compared against the prototype and found 3 real gaps rather than assuming alignment: (1) prototype used a fast per-engine regression fit, diagram calls for a full PF-PMMH fit per engine; (2) prototype kept the library in-memory, diagram stores it in a database; (3) prototype averages the top-5 nearest matches, diagram picks a single best match. Investigated PMMH feasibility before committing to extending it: found `inference/pmmh.py` as it exists only calibrates sigma_v (always against the *pooled* degradation fit — never estimates growth_rate, the actual parameter driving the bias); found Day 8's own 4-engine PMMH test only mixed well on 2/4 engines (accept rates 0.336/0.478 vs. 0.051/0.011 stuck); found the compute cost (N=3200 particles x 1000 MCMC iterations, per engine) doesn't fit the sandbox's 45s-per-call limit. **Agreed plan (Sajjad's explicit choices):** extend PMMH to jointly sample growth_rate + sigma_v per engine (not the lighter regression-only option); detect non-convergence via accept_rate and fall back to the regression-fit growth_rate for any engine that doesn't mix; run the full 100-engine build on Sajjad's own machine, not the sandbox. **Two process changes, both recorded:** (1) Claude now writes code directly, not just thesis prose (extends the Day 8 writing-process change) — Sajjad reviews/approves rather than writing-first; (2) from today, implementation work for anything compute-heavy or needing to run locally goes through Claude Code (the CLI tool) — Cowork hands off a self-contained prompt per step, Sajjad runs it locally and reports back so this file stays in sync; Cowork continues to own planning, design decisions, thesis prose, and this progress file. Original Day-10 plan (final figures, §4.4-§4.6 write-up) moved to Day 11, not dropped — this session's diagnosis/design work displaced it, honestly reflected here rather than silently overwritten. | Day 11: get a Claude Code prompt for the `pmmh.py` joint-parameter extension + `database.py` schema addition, run the full 100-engine library build locally, validate one-shot/periodic at full scale, then resume the original Day 10 plan (§4.4-§4.6, final figures) alongside writing new §3.5/§4.7. |
| 2026-07-17 (Day 11, same session) | **Day 11 complete — all remaining Day 10/11 writing done, two real gaps found and closed.** Ran the full 100-engine pooled/one-shot/periodic comparison in the Cowork sandbox itself, chunked 4-8 engines/call around the 45s-per-call limit (regression-based library rates, not PMMH-calibrated — that stays a separate, local-only follow-up; handoff prompt written to `experiments/day10/claude_code_handoff_pmmh_dbschema.md`). **Decision: one-shot wins outright** — mean abs.err 149.9 vs. periodic's 162.6 vs. pooled's 236.5; fleet RMSE 156.0 vs. 166.8 vs. 244.3; PHM08 total 9.19e12 vs. 9.25e12 vs. 6.90e17; head-to-head 63/100 vs. periodic's 28 (9 ties) — settles what 5 engines couldn't. Honest secondary finding kept in: periodic has lower error variance (37.4 vs. 43.1) despite the worse mean. Two figures built (`fig_library_fullfleet_comparison.png`, `fig_engine78_library_before_after.png`). Wrote §3.5 (NEW, similarity library methodology) and §4.4-§4.7 (§4.7 NEW) after Sajjad approved the plain-language proposals for all five; one citation added and verified (`cai2020similarity`, via Crossref — the paper this design mirrors). **Gap 1, found while writing §3.5:** §3.4 (PMMH) was still a bare TODO stub in `chapt3.tex` despite the code and 4-engine diagnostic being done since Day 8 — the `.tex` prose had simply never been written. Closed same session (motivation, pseudo-marginal MH validity, the likelihood estimator derived from `bootstrap.py`'s actual bracketing order, prior/proposal, the $N$-vs-noise diagnostic, the 4-engine results) — 2 new citations added and verified via Crossref (`andrieu2010pmmh`, `andrieu2009pseudomarginal`). **Gap 2, found while preparing §5.1:** the full-fleet library comparison had only ever tracked point-accuracy metrics — `ci_coverage` was never actually checked for the library variants. Computed it directly (`experiments/day10/library_ci_coverage.py`, all 100 engines): **still 0.000 for one-shot, identical to pooled.** The library fix is a point-accuracy fix, not a calibration fix — §4.6's "UQ capability not yet realised" argument from Day 9 holds exactly as stated, even after today's improvement. Added as its own paragraph in §4.7 rather than left implicit, and became the central, load-bearing fact for Chapter 5. Wrote §5.1 (5 subsections: what the PF delivers, the calibration gap stated head-on, likely sources, other limitations, the digital-twin framing), §5.2 (answers the research objective directly: yes on point-accuracy/architecture, not yet on calibration), §5.3 (6 future-work items, ordered by how directly each targets the calibration gap). Revised Introduction §1.3 (softened "extracts a calibrated RUL distribution" from an implied achieved-result to an explicitly-tested question) and §1.4 (chapter descriptions updated for §3.5/§4.7/Ch.5's actual content); added the missing `\label{ch:intro}`. **Full consistency pass:** brace balance, every `\label`/`\ref`/`\eqref`/`\cite` verified to resolve, across all 5 chapters + bib — clean. Caught and fixed one real cross-reference inconsistency before it became a silent error: §4.7's pooled-dynamics fleet RMSE (244.3, per-engine-reseeded script) vs. §4.5/§4.6's (244.0, continuously-seeded script) — same model, different RNG alignment, already the exact phenomenon documented in `day10_problem_solution.md` §6; added a clarifying footnote to §4.7's table rather than leaving an unexplained-looking discrepancy for a reader to trip over. **LaTeX syntax verification:** the real `main.tex`/class still can't compile in this sandbox (missing `arev.sty`, `algorithm2e.sty`, no network access to fetch either) — built an isolated wrapper (plain `report` class + stub macros for the two missing packages' commands) that `\input`s all 5 chapters directly; compiled clean, 44 pages, zero `! ` errors, only expected first-pass undefined-ref/cite warnings. This narrows the real class's outstanding compile blocker to exactly those two packages, and gives much higher confidence there's no hidden syntax error waiting in the real build. **Also discovered and worked around, not fixed:** this session's `.git/index.lock`/`HEAD.lock` and `thesis/synctest.*` files hit the same FUSE-mount permission quirk documented for `results.db-journal` on Day 4/5/7 — this sandbox can create and overwrite them but not always delete them; `mv`-ing them aside unblocked every `git commit` this session, but `thesis/synctest.{aux,log,out,pdf}` (now 0 bytes) and the `.git/*.lock.bak*` files need manual deletion on Sajjad's machine. **Not yet done, by design:** the PMMH joint (growth_rate, sigma_v) extension and the `engine_parameters` DB table remain local-only (handoff prompt ready); a real local `latexmk` compile against the actual class is still the standing outstanding item. | Day 12 (per timeline): supervisor report — 1-2 pages, one clear figure (the engine-78 before/after or the fleet comparison bar chart are the two strongest candidates), one specific technical question. Also: run the PMMH/DB-schema handoff prompt locally, run a real `latexmk` compile once `arev.sty`/`algorithm2e.sty` are available, and manually clean up the leftover lock/synctest files this sandbox couldn't delete. |
