"""
rul.py — RUL extraction from the particle posterior
=====================================================
Given the particle cloud {h_t^(i)} at the final observed cycle,
propagates each particle forward in time until it crosses the
failure threshold (h = 0), yielding a distribution over RUL.

Outputs (Day 4):
  - RUL median (point estimate)
  - 5th / 95th percentile (90% credible interval)
  - Full empirical distribution (for thesis figures)

TODO (Day 4): implement extract_rul() function.
"""

raise NotImplementedError("rul.py will be implemented on Day 4.")
